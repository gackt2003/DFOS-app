import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Project-1-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
